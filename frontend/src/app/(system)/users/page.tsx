import { ModulePlaceholder } from "@/src/components/system/module-placeholder";

export default function UsersPage() {
  return <ModulePlaceholder title="用户与权限" subtitle="管理用户、角色与权限边界。" relatedTasks={["登录鉴权", "联调回归"]} />;
}
